---
phase: 16-automated-orchestration-reporting
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/integration/Dockerfile.test
  - tests/integration/docker-compose.yml
  - tests/integration/requirements-test.txt
  - tests/integration/scripts/run-sil-tests.sh
  - tests/integration/tests/test_orchestration.py
autonomous: true
requirements:
  - SIL-03
user_setup: []
estimate:
  tokens: 18000
  raw_tokens: 12000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - Starting the compose test runner automatically invokes the Phase 14 bridge SIL suite and Phase 15 Home Assistant integration SIL suite as separate sequential pytest runs.
    - Missing optional test dependencies or either suite's failures produce a visible nonzero runner/container result; no suite is converted into a skip or success.
    - One run produces host-visible per-suite evidence, a merged SIL JUnit report, and coverage summaries in XML and HTML for both bridge backend and bundled integration source roots.
    - The runner is reproducible locally and in CI from the existing Docker image without requiring a live Bluetooth adapter, D-Bus socket, or external bridge for these SIL suites.
  artifacts:
    - tests/integration/scripts/run-sil-tests.sh
    - tests/integration/tests/test_orchestration.py
    - tests/integration/requirements-test.txt
    - tests/integration/docker-compose.yml
    - tests/integration/Dockerfile.test
  key_links:
    - docker-compose test-runner command -> run-sil-tests.sh -> focused bridge and HA pytest invocations
    - requirements-test.txt -> Dockerfile.test pip install -> dependency preflight before suite execution
    - pytest-cov append data -> coverage report/xml/html outputs -> compose-mounted reports directory
    - bridge-junit.xml and integration-junit.xml -> standard-library merge -> sil-junit.xml
---

<objective>
Wire the Phase 14 bridge SIL suite and Phase 15 Home Assistant integration SIL suite into the compose test runner and generate unified, host-visible test and coverage reports.

Purpose: Make the completed SIL suites executable as one repeatable local/CI check while preserving failures caused by missing optional dependencies or test regressions.
Output: An image dependency update, an executable fail-reporting runner, compose report mounting/startup wiring, and static orchestration contract tests.
</objective>

<execution_context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/14-bridge-backend-sil-testing/14-01-SUMMARY.md
@.planning/phases/15-ha-integration-sil-testing/15-01-SUMMARY.md
@.planning/phases/16-automated-orchestration-reporting/16-01-CONTEXT.md
@.planning/phases/16-automated-orchestration-reporting/16-01-RESEARCH.md
@tests/integration/Dockerfile.test
@tests/integration/docker-compose.yml
@tests/integration/requirements-test.txt
@pytest.ini
</execution_context>

<context>
The existing `test-runner` only tails `/dev/null`, so compose startup does not execute any test. Phase 14's focused module is `modules/bridge/tests/test_bridge_sil.py`; Phase 15's focused module is `modules/integration/tests/test_ha_integration_sil.py`. Both are in-process suites and should remain independent of the HAOS VM. Per D-01, reuse the existing image and compose stack. Per D-02, run both suite commands sequentially, continue to the second after a first-suite failure, and aggregate the exit status. Per D-03, preflight the shared optional dependencies and fail clearly when they are absent. Per D-04, append coverage across both runs, preserve separate JUnit files, and merge them into one report using the standard library. Per D-05, do not modify production code or the SIL tests themselves.
</context>

<tasks>

<task type="tracer">
  <name>Task 1: Trace the bridge SIL suite through the container runner</name>
  <files>tests/integration/requirements-test.txt, tests/integration/Dockerfile.test, tests/integration/docker-compose.yml, tests/integration/scripts/run-sil-tests.sh</files>
  <action>Add the verified `pytest-cov` test-only dependency to the shared requirements consumed by `Dockerfile.test`. Create an executable POSIX runner that creates `/reports`, performs an explicit preflight for pytest, pytest-cov, and `pytest-homeassistant-custom-component`, erases stale coverage data, runs `modules/bridge/tests/test_bridge_sil.py` with the existing root pytest configuration and a per-suite JUnit file, and records the command status without suppressing failures. Change the compose `test-runner` command from the idle tail process to this runner and mount `./reports` at `/reports`. Preserve the existing HAOS service, repository mount, and environment contract. Cite D-01, D-02, D-03, SIL-03.
  <verify>
    <automated>docker compose -f tests/integration/docker-compose.yml build test-runner && docker compose -f tests/integration/docker-compose.yml run --rm test-runner</automated>
  </verify>
  <done>The built test-runner starts the real runner, preflights required dependencies, executes the bridge SIL suite, writes its first JUnit/coverage evidence under the mounted reports directory, and returns nonzero for a preflight or bridge failure.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Expand the runner to HA SIL execution and unified reporting</name>
  <files>tests/integration/scripts/run-sil-tests.sh</files>
  <behavior>
    - The bridge SIL suite runs before the Home Assistant integration SIL suite in separate pytest processes.
    - A bridge failure does not prevent the HA suite from running, and a failure in either suite makes the runner exit nonzero.
    - Coverage data from both suites is accumulated only for `modules/bridge/backend` and `modules/integration/custom_components/bl_haos`.
    - Separate JUnit files are retained and merged into one `sil-junit.xml`; coverage summary, XML, and HTML artifacts are written even when test execution records failures where the coverage tool has usable data.
    - Missing `pytest-homeassistant-custom-component` or `pytest-cov` causes a clear preflight failure before tests are reported as successful.
  </behavior>
  <action>Extend the runner with the Phase 15 focused command and `--cov-append` options for both explicit source roots. Capture each pytest exit code while continuing to the second suite, run standard-library XML merging for the two per-suite JUnit files, generate terminal, XML, and HTML coverage reports, and exit with the aggregate nonzero status after report generation. Do not use `|| true`, unconditional success, dependency-based skipping, or stale coverage data. Cite D-02, D-03, D-04, SIL-03.
  <verify>
    <automated>docker compose -f tests/integration/docker-compose.yml run --rm test-runner; $exit = $LASTEXITCODE; if (-not (Test-Path tests/integration/reports/sil-junit.xml)) { throw 'Missing merged SIL report' }; if (-not (Test-Path tests/integration/reports/coverage.xml)) { throw 'Missing coverage XML' }; if ($exit -eq 0) { Write-Output 'Runner passed'; } else { throw "Runner reported failure: $exit" }</automated>
  </verify>
  <done>One container run executes both focused SIL suites sequentially, preserves individual evidence, emits merged JUnit plus XML/HTML/terminal coverage for both source roots, and accurately reports any dependency or test failure through its exit code.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Lock the compose and runner contract with static tests</name>
  <files>tests/integration/tests/test_orchestration.py</files>
  <behavior>
    - Compose defines the test-runner startup command as the SIL runner and mounts the reports output directory.
    - The runner references both focused SIL modules in bridge-before-integration order, performs dependency/coverage setup, and contains no unconditional success path.
    - The Docker test image installs the shared requirements that include the Home Assistant harness and coverage plugin.
    - The contract test remains runnable without Docker, a HAOS VM, Bluetooth hardware, D-Bus, or optional local Python packages.
  </behavior>
  <action>Create a static repository-level orchestration test using YAML/text inspection consistent with existing test style. Assert the exact compose command/report mount, both suite paths and ordering, coverage output names/source roots, explicit dependency preflight, and nonzero failure propagation in the runner. Assert the Dockerfile installs `requirements-test.txt` and that the requirements file contains the verified optional harness and `pytest-cov`. Keep this test independent of importing either optional runtime package. Cite D-01, D-03, D-04, D-05, SIL-03.
  <verify>
    <automated>pytest tests/integration/tests/test_orchestration.py -q</automated>
  </verify>
  <done>The orchestration contract is regression-tested without launching Docker, and the test fails if compose returns to an idle runner, omits either SIL suite, hides failures, or stops producing unified reports.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Compose host -> test-runner container | Host-controlled mounts and commands determine which source and report paths are executed. |
| Docker image build -> Python test environment | PyPI dependencies determine whether optional SIL harnesses and coverage tooling are available. |
| SIL runner -> pytest suites and report files | Test outcomes and generated XML/coverage artifacts cross the shell process boundary. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-16-01 | Tampering | `docker-compose.yml` runner command and report mount | medium | mitigate | Static orchestration tests assert the runner command, focused suite paths, and host-visible report mount. |
| T-16-02 | Denial of service | `run-sil-tests.sh` failure aggregation | high | mitigate | Capture both suite statuses, continue to the second suite for evidence, generate reports, and return the aggregate nonzero status. |
| T-16-03 | Tampering | PyPI test dependencies | high | mitigate | Use only the verified `pytest-homeassistant-custom-component` and `pytest-cov` packages from the research audit; fail preflight when unavailable. |
| T-16-04 | Information disclosure | Mounted test reports | low | mitigate | Restrict report inputs to deterministic SIL suites and source roots; do not write tokens or live-host credentials into artifacts. |
| T-16-SC | Tampering | npm/pip/cargo installs | high | mitigate | Package legitimacy gate is satisfied by the verified PyPI audit; no unverified package is introduced. |
</threat_model>

## Source Audit

| Source | Item | Plan | Status | Notes |
|---|---|---|---|---|
| GOAL | Wire Phase 14 and Phase 15 tests into the CI/local orchestrator | 16-01 | COVERED | Tasks 1-2 replace the idle runner and execute both focused suites. |
| GOAL | Generate test coverage reports | 16-01 | COVERED | Task 2 accumulates coverage and writes terminal, XML, and HTML reports. |
| REQ | SIL-03: execute both suites sequentially and output a unified test report | 16-01 | COVERED | Tasks 1-3 define ordering, status aggregation, per-suite JUnit evidence, and merged `sil-junit.xml`. |
| RESEARCH | Reuse existing Docker image, compose stack, root pytest discovery, and Phase 14/15 suite paths | 16-01 | COVERED | Task 1 updates only existing orchestration surfaces; Task 3 locks the contract. |
| RESEARCH | Account for missing `pytest-homeassistant-custom-component` and add verified coverage tooling | 16-01 | COVERED | Tasks 1-2 preflight dependencies and fail visibly; the package audit records both verified PyPI packages. |
| CONTEXT | D-01 through D-05 | 16-01 | COVERED | Every decision is cited in task actions/behavior and reflected in the must-haves. |

<verification>
Run the static contract check from the repository root, then build and execute the container runner:

1. `pytest tests/integration/tests/test_orchestration.py -q`
2. `docker compose -f tests/integration/docker-compose.yml build test-runner`
3. `docker compose -f tests/integration/docker-compose.yml run --rm test-runner`
4. Confirm `tests/integration/reports/sil-junit.xml`, `coverage.xml`, and HTML coverage output exist.
5. Confirm a deliberately missing dependency or failing focused test causes a nonzero runner exit rather than a passing container.

The canonical container command is allowed to fail until the image is rebuilt with all shared requirements. The local host environment's known missing `pytest-homeassistant-custom-component`/Playwright packages must remain an explicit environment failure, not a reason to weaken the runner.
</verification>

<success_criteria>
- `test-runner` executes both focused SIL suites automatically in deterministic order.
- Missing optional dependencies and suite failures remain visible through logs and a nonzero exit code.
- Per-suite JUnit files, merged `sil-junit.xml`, coverage XML, HTML, and terminal coverage are generated in the mounted reports directory when the corresponding tools have run.
- Coverage includes only the Phase 14 bridge backend and Phase 15 bundled integration source roots and is not inflated by stale prior-run data.
- The static orchestration test protects the compose, image, runner, dependency, ordering, and failure-propagation contract.
</success_criteria>

<output>
Create `.planning/phases/16-automated-orchestration-reporting/16-01-SUMMARY.md` when done.
</output>