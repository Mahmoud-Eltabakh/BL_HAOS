---
phase: "07"
plan: "02"
wave: 1
depends_on:
  - "07-01"
files_modified:
  - backend/bl_haos/ha/player.py
  - backend/bl_haos/ha/__init__.py
  - backend/bl_haos/main.py
  - tests/test_media_player.py
autonomous: true
requirements:
  - HA-02
  - HA-03
  - HA-04
---

# Plan 07-02: Media Player Playback Pipeline for TTS, Web Radio & Volume Sync

## Goal

Implement `MediaPlayerBridge` handling playback stream dispatch (`play_media` for Home Assistant TTS announcements and web radio streams), pause, stop, and bi-directional volume synchronization.

## Must Haves

1. `MediaPlayerBridge` in `backend/bl_haos/ha/player.py` handling playback stream spawning (via `pw-play` or subprocess PCM streamer), volume normalization, and state transitions (`idle`, `playing`, `paused`, `off`).
2. Integration in `backend/bl_haos/main.py` tying BluetoothManager events and MQTT Discovery together.
3. Automated test suite `tests/test_media_player.py`.

## Tasks

<task id="07-02-01">
<title>Implement MediaPlayerBridge playback engine</title>
<description>
Author:
- `backend/bl_haos/ha/player.py`: `MediaPlayerBridge` managing playback state, stream spawning, volume control, and MQTT message routing.
- Integrate bridge into `backend/bl_haos/main.py` lifespan.
</description>
<files>
- backend/bl_haos/ha/player.py
- backend/bl_haos/ha/__init__.py
- backend/bl_haos/main.py
- tests/test_media_player.py
</files>
<verify>
<automated>pytest tests/test_media_player.py</automated>
</verify>
</task>

## Verification Criteria

- [ ] `pytest tests/test_media_player.py` passes.
- [ ] TTS streams and URL media play commands route without errors.
