---
phase: "05"
plan: "02"
wave: 1
depends_on:
  - "05-01"
files_modified:
  - backend/bl_haos/api/ws.py
  - tests/test_ws.py
autonomous: true
requirements:
  - API-02
---

# Plan 05-02: Real-Time WebSocket Event Bus & Telemetry Gateway

## Goal

Implement the WebSocket gateway in `backend/bl_haos/api/ws.py` providing real-time event streaming for discovery, signal changes, connection states, and audio telemetry to connected Ingress clients.

## Must Haves

1. `ConnectionManager` handling multiple client WebSocket connections, disconnect cleanups, and broadcast queues.
2. Endpoint `/ws` streaming JSON events (`device_discovered`, `device_updated`, `device_removed`, `adapter_updated`, `reconnect_event`).
3. Automated test suite `tests/test_ws.py` validating WebSocket connect, receive, and broadcast delivery.

## Tasks

<task id="05-02-01">
<title>Implement WebSocket ConnectionManager and /ws route</title>
<description>
Create `backend/bl_haos/api/ws.py`:
- `ConnectionManager` with `connect()`, `disconnect()`, `broadcast(event, data)` methods
- Route handler `@router.websocket("/ws")` supporting live message streaming
</description>
<files>
- backend/bl_haos/api/ws.py
- tests/test_ws.py
</files>
<verify>
<automated>pytest tests/test_ws.py</automated>
</verify>
</task>

## Verification Criteria

- [ ] `pytest tests/test_ws.py` passes.
- [ ] WebSocket broadcasts events in real-time to active clients.
