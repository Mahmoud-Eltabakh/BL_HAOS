---
phase: 15-ha-integration-sil-testing
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/integration/requirements-test.txt
  - modules/integration/tests/conftest.py
  - modules/integration/tests/fixtures/speakers_initial.json
  - modules/integration/tests/fixtures/speakers_after_eviction.json
  - modules/integration/tests/test_ha_integration_sil.py
  - modules/integration/custom_components/bl_haos/media_player.py
autonomous: true
requirements:
  - SIL-02
  - SIL-04
user_setup: []
estimate:
  tokens: 18000
  raw_tokens: 18000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - A Home Assistant core test loads the bundled bl_haos custom component and creates one native media_player entity per valid speaker in the mocked authoritative snapshot.
    - Checked-in JSON fixtures provide deterministic initial and reduced /speakers responses without a live bridge, HAOS VM, Bluetooth adapter, D-Bus socket, or network service.
    - Refreshing from a snapshot that omits a previously present speaker removes that speaker from the active Home Assistant entity set and leaves remaining entities usable (WR-01).
    - Config-entry validation, setup, websocket/listener cleanup, and unload behavior are covered by executable integration SIL tests.
    - The focused integration SIL suite and the existing repository test suite run from the repository root under the shared pytest configuration.
  artifacts:
    - modules/integration/tests/test_ha_integration_sil.py
    - modules/integration/tests/conftest.py
    - modules/integration/tests/fixtures/speakers_initial.json
    - modules/integration/tests/fixtures/speakers_after_eviction.json
  key_links:
    - mocked GET /api/native/identity and /api/native/speakers -> BLHAOSClient.async_initialize -> entry.runtime_data -> media_player.async_setup_entry
    - BLHAOSClient._async_refresh_snapshot absent-address notification -> media_player entity registry/map removal -> Home Assistant entity disappearance
    - ConfigEntry unload -> async_unload_entry -> BLHAOSClient.async_close -> websocket task/listener cleanup
---

<objective>
Build the Home Assistant integration SIL suite for deterministic bridge speaker snapshots and native media-player entity lifecycles, including the WR-01 stale-speaker eviction path.

Purpose: Prove the custom integration against Home Assistant core seams in-process, so snapshot parsing, entity creation, removal, config-flow/setup failure behavior, and unload cleanup are regression-tested without physical Bluetooth or a running bridge.
Output: A focused HA-core pytest module, checked-in bridge snapshot fixtures, the test-runner dependency needed to execute it, and the minimal media-player lifecycle wiring required to evict stale entities.
</objective>

<execution_context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/15-ha-integration-sil-testing/15-01-CONTEXT.md
@.planning/phases/15-ha-integration-sil-testing/15-01-RESEARCH.md
@modules/integration/custom_components/bl_haos/__init__.py
@modules/integration/custom_components/bl_haos/client.py
@modules/integration/custom_components/bl_haos/config_flow.py
@modules/integration/custom_components/bl_haos/media_player.py
@modules/integration/custom_components/bl_haos/const.py
@modules/integration/tests/test_package_layout.py
@tests/integration/requirements-test.txt
@pytest.ini
</execution_context>

<context>
The bridge contract is a static `GET /api/native/identity` response followed by `GET /api/native/speakers`, then native websocket updates. The client already deletes cache entries absent from a refreshed snapshot and notifies listeners, but the media-player platform currently only adds or writes entities for listener callbacks; it does not remove an entity when its speaker disappears. Per D-01 and D-02, use the Home Assistant custom-component pytest harness and checked-in JSON snapshots. Per D-03, make the SIL test assert active entity eviction and wire the smallest production removal path needed for that assertion. Per D-04, do not modify docker-compose orchestration or unified reporting owned by Phase 16.
</context>

<tasks>

<task type="tracer">
  <name>Task 1: Trace a mocked bridge snapshot into Home Assistant media-player entities</name>
  <files>tests/integration/requirements-test.txt, modules/integration/tests/conftest.py, modules/integration/tests/fixtures/speakers_initial.json, modules/integration/tests/test_ha_integration_sil.py</files>
  <action>Add the verified `pytest-homeassistant-custom-component` test dependency to the shared integration test requirements. Create reusable test transport fixtures that load the checked-in identity and initial `/api/native/speakers` JSON, return aiohttp-compatible async response context managers, and prevent real network access. Enable the bundled `custom_components.bl_haos` component through Home Assistant's test harness, create one config entry with the endpoint/token contract, run the real entry setup, and assert that each valid authoritative speaker creates a native `media_player` entity with normalized unique identity, name, availability, and device metadata. Keep credentials out of fixture assertions and ensure the client's websocket task is cleaned up by the fixture or entry unload. Cite D-01, D-02, SIL-02, and SIL-04.</action>
  <verify>
    <automated>pytest modules/integration/tests/test_ha_integration_sil.py -q -k snapshot_creation</automated>
  </verify>
  <done>The focused tracer loads the actual custom component into Home Assistant, serves only the static bridge fixtures, and observes the expected media-player entities from the initial snapshot without external services.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Prove and implement stale-speaker entity eviction</name>
  <files>modules/integration/tests/fixtures/speakers_after_eviction.json, modules/integration/tests/test_ha_integration_sil.py, modules/integration/custom_components/bl_haos/media_player.py</files>
  <behavior>
    - A refreshed snapshot containing the original speakers keeps their entities and updates their authoritative state.
    - A refreshed snapshot omitting one previously present address removes that address from `BLHAOSClient.speakers` and from the active Home Assistant media-player entity set.
    - The remaining speaker continues to expose its stable unique ID and can receive a state update after eviction.
    - Repeated refresh/listener notifications do not recreate the evicted entity or leave duplicate entities behind.
  </behavior>
  <action>Add the reduced static snapshot fixture and write the WR-01 regression test against the real client refresh/listener path. Then update `media_player.async_setup_entry` so its address callback distinguishes an existing speaker, a newly cached speaker, and an address no longer present in `client.speakers`; remove the corresponding Home Assistant entity and its local tracking entry on eviction, while preserving normal state writes for retained speakers. Use Home Assistant's supported entity-removal lifecycle API and keep the client callback contract unchanged unless the test demonstrates that a narrower event distinction is required. Cite D-02, D-03, SIL-02, and SIL-04.</action>
  <verify>
    <automated>pytest modules/integration/tests/test_ha_integration_sil.py -q -k eviction</automated>
  </verify>
  <done>WR-01 is executable: removing a speaker from the authoritative snapshot removes its native media-player entity, preserves remaining entities, and does not create duplicates on repeated callbacks.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Expand config-flow, setup-failure, and unload lifecycle coverage</name>
  <files>modules/integration/tests/test_ha_integration_sil.py</files>
  <behavior>
    - A valid config-flow endpoint/token validates bridge identity and creates the expected single integration entry data.
    - Invalid endpoint schemes, malformed endpoints, missing tokens, and an unexpected bridge identity produce the existing cannot-connect behavior without starting entity setup.
    - Unloading a configured entry removes its entities, cancels the client's websocket task, and releases update listeners.
    - The complete integration test module remains independent of test order and does not contact a live endpoint.
  </behavior>
  <action>Extend the SIL module using the existing `config_flow.py` validation rules and Home Assistant config-entry flow helpers. Mock identity responses for successful and failed validation, assert the resulting flow outcome/error and stored endpoint normalization without asserting token contents, then exercise `hass.config_entries.async_unload` and verify entity removal plus client close/task cleanup. Keep this coverage focused on current behavior; do not add orchestration, coverage reporting, or live HAOS calls, per D-04. Cite D-01, D-04, SIL-02, and SIL-04.</action>
  <verify>
    <automated>pytest modules/integration/tests/test_ha_integration_sil.py -q</automated>
  </verify>
  <done>The integration SIL module covers snapshot creation, WR-01 eviction, config-flow connection outcomes, and clean entry unload with deterministic mocked transport.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Home Assistant integration -> mocked bridge HTTP/WebSocket transport | The integration consumes untrusted remote identity, snapshot, and event-shaped data even though the SIL transport is local to the test process. |
| Config flow user input -> endpoint normalization and identity validation | Endpoint and token fields enter the integration before a config entry is persisted. |
| Snapshot address -> Home Assistant entity identity/removal | A bridge-provided Bluetooth address controls entity creation, update routing, and eviction. |
| Test dependency installation -> test runner | PyPI packages affect executable test code and must remain limited to the verified test-only package. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|----------|----------|-----------|----------|-------------|-----------------|
| T-15-01 | Spoofing | `config_flow.py` identity validation | high | mitigate | Test that only the expected bridge ID creates an entry and malformed/unsupported endpoints remain rejected. |
| T-15-02 | Tampering | `client.py` snapshot parsing and address normalization | medium | mitigate | Feed deterministic valid, invalid, and omitted speaker records and assert only normalized valid sinks reach the entity platform. |
| T-15-03 | Denial of service | `media_player.py` dynamic entity lifecycle | medium | mitigate | Assert repeated updates and eviction do not duplicate entities or leave stale registrations after removal. |
| T-15-04 | Information disclosure | SIL fixtures and config-flow assertions | low | mitigate | Use non-secret fixture tokens, avoid token assertions in output, and assert only normalized endpoint/identity behavior. |
| T-15-SC | Tampering | PyPI test dependency | high | mitigate | Use only the verified `pytest-homeassistant-custom-component` package recorded in the phase research/context audit; do not add unverified packages. |
</threat_model>

## Source Audit

| Source | Item | Plan | Status | Notes |
|---|---|---|---|---|
| GOAL | Build Home Assistant integration SIL coverage for entity lifecycle management | 15-01 | COVERED | Tasks 1-3 create, update, and unload entities through Home Assistant core. |
| REQ | SIL-02: load `bl_haos`, create media-player entities, and verify strict snapshot eviction | 15-01 | COVERED | Tasks 1-3, with the WR-01 test and lifecycle fix in Task 2. |
| REQ | SIL-04: provide stable authoritative bridge mock data | 15-01 | COVERED | Checked-in initial and reduced JSON fixtures in Tasks 1-2. |
| RESEARCH | In-process HA custom-component harness and mocked aiohttp transport | 15-01 | COVERED | Task 1. |
| RESEARCH | Client cache eviction must reach platform entity removal | 15-01 | COVERED | Task 2. |
| CONTEXT | D-01 through D-04 | 15-01 | COVERED | Each task cites the decisions it implements. |

<verification>
Run from `F:\Workspace\BL-HAOS`:

1. `pytest modules/integration/tests/test_ha_integration_sil.py -q -k snapshot_creation`
2. `pytest modules/integration/tests/test_ha_integration_sil.py -q -k eviction`
3. `pytest modules/integration/tests/test_ha_integration_sil.py -q`
4. `pytest tests/ -q`

The focused integration suite must pass before the full repository regression command. Tests must not require a live Home Assistant instance, bridge endpoint, Bluetooth adapter, D-Bus socket, credentials, or network access.
</verification>

<success_criteria>
- `modules/integration/tests/test_ha_integration_sil.py` loads the actual custom component and covers static snapshot creation, config-flow validation, stale-speaker eviction, and entry unload.
- `speakers_initial.json` and `speakers_after_eviction.json` are stable authoritative bridge fixtures used by the tests rather than duplicated inline payloads.
- WR-01 is protected by an assertion on the active Home Assistant entity set, not only the client cache.
- The focused integration SIL suite and the complete repository test suite are executable with the shared pytest configuration.
- Phase 16 remains the owner of docker-compose test-runner orchestration and unified reporting.
</success_criteria>

<output>
Create `.planning/phases/15-ha-integration-sil-testing/15-01-SUMMARY.md` when done.
</output>
