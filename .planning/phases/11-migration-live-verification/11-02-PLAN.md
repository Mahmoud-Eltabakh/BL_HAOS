---
phase: 11-migration-live-verification
plan: 02
type: execute
wave: 2
depends_on:
  - 11-01
files_modified:
  - query_ha.py
  - tests/test_query_ha.py
  - DOCS.md
  - README.md
  - CHANGELOG.md
autonomous: false
requirements:
  - MIG-03
user_setup:
  - service: Home Assistant OS at 192.168.1.21
    why: "The live Raspberry Pi must be reachable, the BL-HAOS add-on must be installed, and its Logitech Bluetooth speaker must be trusted and connected."
    env_vars:
      - name: HA_TOKEN
        source: "Home Assistant user profile -> Security -> Long-Lived Access Tokens; provide only through the local process environment, never source control, command arguments, logs, or chat."
      - name: HA_URL
        source: "Set to http://192.168.1.21:8123 for the already configured live HAOS host."
  - service: Home Assistant media source or controlled test audio
    why: "A short known-audible media item or approved TTS source is needed to prove physical speaker playback after the native entity command succeeds."
estimate:
  tokens: 34000
  raw_tokens: 34000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "The repository contains no reusable Home Assistant bearer credential, and the live verification utility never prints or persists a supplied credential."
    - "A live operator can first inspect the configured Home Assistant host read-only, then explicitly deploy/restart only the verified BL-HAOS add-on when needed."
    - "The live Logitech Bluetooth speaker appears through the BL-HAOS native integration as a `media_player`, receives one real Home Assistant media command, and an operator confirms audible playback."
  artifacts:
    - path: query_ha.py
      provides: "Environment-authenticated, read-only-by-default HA WebSocket verification and explicitly gated add-on lifecycle operations."
    - path: tests/test_query_ha.py
      provides: "Credential-safety and command-payload tests for the live verification utility."
    - path: DOCS.md
      provides: "Native migration, integration-installation, rollback, and live Logitech verification runbook."
  key_links:
    - from: query_ha.py
      to: http://192.168.1.21:8123/api/websocket
      via: "HA_TOKEN environment-only WebSocket authentication and bounded read-only preflight."
      pattern: "HA_TOKEN"
    - from: query_ha.py
      to: "Home Assistant media_player.play_media service"
      via: "An explicit target entity and caller-supplied media identifier produce the live control request."
      pattern: "media_player.play_media"
    - from: DOCS.md
      to: backend/ha_integration/custom_components/bl_haos
      via: "The runbook instructs a safe packaged-component installation/reload and validates native entity ownership before playback."
      pattern: "BL-HAOS Bluetooth Audio"
---

<objective>
Remove the embedded HA credential from the live helper, package and install the native integration through the established non-destructive add-on path, and verify live Logitech speaker playback on the configured HAOS host.

Purpose: Fulfill MIG-03 with a reproducible, secret-safe operator runbook and a real Home Assistant command against the existing Raspberry Pi rather than a simulated backend test.
Output: A guarded HA WebSocket utility, utility tests, updated migration documentation, and recorded live evidence for the native Logitech entity and audible playback.
</objective>

<execution_context>
@~/.copilot/gsd-core/workflows/execute-plan.md
@~/.copilot/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-native-integration-foundation/09-01-SUMMARY.md
@.planning/phases/10-native-player-control-state-sync/10-02-SUMMARY.md
@.planning/phases/11-migration-live-verification/11-01-SUMMARY.md
@query_ha.py
@DOCS.md
@README.md
@backend/ha_integration/custom_components/bl_haos/config_flow.py
@rootfs/etc/s6-overlay/s6-rc.d/00-init-environment/up

Official Home Assistant guidance consulted during planning:
- https://developers.home-assistant.io/docs/apps/configuration/
- https://developers.home-assistant.io/docs/config_entries_config_flow_handler/
- https://developers.home-assistant.io/docs/core/entity/media-player/
- https://developers.home-assistant.io/docs/core/integration-quality-scale/rules/test-before-configure/
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Make the HAOS verification utility credential-safe and read-only by default</name>
  <files>query_ha.py, tests/test_query_ha.py</files>
  <behavior>
    - Test 1: the utility obtains its Home Assistant URL and bearer credential only from `HA_URL` and `HA_TOKEN` process environment variables, rejects missing values, and contains no credential-shaped literal or default token.
    - Test 2: its default preflight authenticates over the HA WebSocket and requests only add-on metadata, BL-HAOS config-entry state, entity/device registry evidence, and current target entity state; it never reloads stores, rebuilds, stops, starts, or restarts an add-on.
    - Test 3: lifecycle operations require both an explicit subcommand and `--apply`, resolve the add-on slug from Supervisor metadata before acting, and refuse an add-on slug that is not BL-HAOS.
    - Test 4: the playback command requires an explicit BL-HAOS native `media_player` entity id plus a caller-supplied media identifier/type, sends the Home Assistant service request, polls bounded state evidence, and redacts authorization/media values from normal and error output.
  </behavior>
  <precondition>The previously embedded Home Assistant long-lived access token is revoked and replaced only in the local `HA_TOKEN` environment variable before this utility is run.</precondition>
  <action>Replace the one-off destructive sequence in `query_ha.py` with a small argparse-based HA WebSocket utility. Read `HA_URL` and `HA_TOKEN` from the process environment at runtime; never embed, echo, serialize, or include either value in exception text. Make `preflight` the default mode and limit it to authenticated read-only calls that identify the configured add-on, installed `bl_haos` config entry, native `media_player` entities, and their Bluetooth-address diagnostics. Put store reload, add-on rebuild, stop, and start behind named subcommands that require `--apply`, verify the returned add-on slug before every mutation, and emit only operation/status identifiers. Add a `play-media` mode that requires an explicit entity id and caller-supplied media URL or media-source identifier/type, calls Home Assistant's standard `media_player.play_media` service, then polls only a bounded set of non-sensitive state fields for acknowledgement. Structure WebSocket request/response handling so request IDs are correlated and unrelated events are ignored. Add mocked WebSocket tests covering missing environment, secret redaction, read-only default, mutation gating, target validation, and command shape. Do not provide a default public media URL or a way to pass a token on the command line.</action>
  <verify>
    <automated>PYTHONPATH=. pytest tests/test_query_ha.py -v</automated>
  </verify>
  <done>The repository has no embedded HA bearer credential, and the local helper can perform a redacted read-only preflight while requiring deliberate guarded syntax for mutation and playback operations.</done>
</task>

<task type="auto">
  <name>Task 2: Package the integration safely and prove native Logitech playback on HAOS</name>
  <files>DOCS.md, README.md, CHANGELOG.md</files>
  <precondition>The HAOS host at `192.168.1.21` is reachable; `HA_URL` and a newly issued `HA_TOKEN` are set locally; the Logitech speaker is powered, paired, trusted, connected as an A2DP sink, and physically audible.</precondition>
  <action>Update installation and migration documentation so MQTT is described only as disabled-by-default optional interoperability, while the bundled custom integration and native REST/WebSocket bridge are the supported entity transport. Document the exact safe migration order: take an HA backup; deploy the revised add-on; start it so the ownership-scoped installer replaces only `custom_components/bl_haos`; reload or restart Home Assistant Core so the component is discovered; configure or confirm the single BL-HAOS entry; remove stale MQTT-discovery entities only after the native entity is present; and use the documented rollback by restoring the backup or re-enabling interoperability only for compatibility consumers. Record the live validation procedure against the configured host: run the helper preflight with environment-only credentials; capture non-secret evidence that the config entry is loaded and a Logitech-addressed native `media_player` exists; run a guarded add-on deployment only if the reported version/install status requires it; invoke `play-media` with a short approved media item; capture returned service/state evidence; and confirm audibly at the physical speaker. Add a release-note entry that calls out the MQTT-discovery retirement, diagnostics, and the required credential rotation. Keep the expected live evidence in the Phase 11 summary rather than committing tokens, raw WebSocket frames, signed URLs, full device identifiers, or other sensitive runtime output.</action>
  <verify>
    <automated>PYTHONPATH=. pytest tests/test_addon_config.py tests/test_custom_integration.py tests/test_native_transport.py tests/test_query_ha.py -v</automated>
    <human-check>With `HA_URL=http://192.168.1.21:8123` and a newly issued local `HA_TOKEN`, run the safe preflight, confirm the Logitech native entity is owned by the BL-HAOS integration, execute the guarded `play-media` command with an approved short audio item, and confirm the physical speaker emits that audio. Record only redacted IDs/statuses in the Phase 11 summary.</human-check>
  </verify>
  <done>The bundled integration is installed without replacing unrelated Home Assistant configuration, the live Logitech Bluetooth speaker exists as a BL-HAOS native media player on `192.168.1.21`, a real Home Assistant media command reaches it, and audible playback is confirmed with redacted evidence.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Local verification shell to Home Assistant WebSocket | A bearer credential authorizes Supervisor, registry, and media service operations on the live HAOS host. |
| Verification utility to Supervisor add-on lifecycle API | Explicit lifecycle calls can rebuild or restart a live add-on. |
| Home Assistant media command to Bluetooth speaker | A supplied media item becomes a playback request routed through the native bridge and PipeWire runtime. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-11-06 | S | HA WebSocket credential | critical | mitigate | Revoke the embedded credential, require environment-only `HA_TOKEN`, redact all output, and prohibit CLI-token arguments or persistence. |
| T-11-07 | T | Supervisor lifecycle operations | high | mitigate | Make preflight read-only; require an explicit named operation plus `--apply`, verify the add-on slug, and keep a backup/rollback sequence in the runbook. |
| T-11-08 | I | Logs, tests, and Phase summary | high | mitigate | Test redaction paths and record only status, timestamp, redacted entity evidence, and audible result. |
| T-11-09 | D | Live playback command | medium | mitigate | Require an explicit native entity and short approved media item, use bounded state polling, and leave no unattended playback process or retry loop in the helper. |
| T-11-10 | R | Physical playback evidence | medium | mitigate | Correlate the service response with native entity state and an audible human confirmation before declaring MIG-03 complete. |
| T-11-SC | Tampering | Package installs | high | accept | No npm, pip, or cargo package installation is planned. |
</threat_model>

<source_audit>

SOURCE | ID | Feature | Plan | Status | Notes
GOAL | -- | Remove unsupported discovery dependency and prove native speaker control on HAOS | 11-01, 11-02 | COVERED | This plan performs the safety-hardened deployment and live proof.
REQ | MIG-01 | Deprecate MQTT discovery; retain only justified optional interoperability | 11-01 | COVERED | Delivered by the preceding plan.
REQ | MIG-02 | Surface native-installation and connection status in Ingress and diagnostics | 11-01 | COVERED | Delivered by the preceding plan.
REQ | MIG-03 | Verify Logitech native player and real command on live Raspberry Pi | 11-02 | COVERED | Tasks 1 and 2 use the live HAOS host with secret-safe control and physical confirmation.
RESEARCH | -- | Sensitive diagnostics must be redacted; config flows validate live transport before entry creation | 11-02 | COVERED | Task 1 redacts all credential/media values; Task 2 proves configured native ownership before playback.
CONTEXT | -- | No Phase 11 context artifact exists | 11-01, 11-02 | COVERED | No locked decisions or deferred ideas apply.
</source_audit>

<verification>
Run the focused automated suites before the live host. On the actual HAOS host, use the environment-only credential flow to inspect first, deploy only when preflight says the installed version or component state needs it, then issue one controlled `media_player.play_media` request and capture redacted service/state plus audible proof.
</verification>

<success_criteria>
MIG-03 is complete only when the configured HAOS host at `192.168.1.21` shows the Logitech Bluetooth A2DP speaker as a BL-HAOS native `media_player`, the standard Home Assistant media command returns successfully, and an operator hears playback. The prior embedded credential is revoked and no secret-bearing live evidence enters the repository.
</success_criteria>

<output>
Create `.planning/phases/11-migration-live-verification/11-02-SUMMARY.md` when done.
</output>